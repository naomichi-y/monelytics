# monelytics

家計簿の Web アプリ。PHP 8.5 / Laravel 13 / MariaDB 12.3 / Redis 8 を Docker Compose で
動かす。環境構築は [README.md](README.md) を参照。

```
docker compose exec -u webapp php php artisan test     # テスト
docker compose exec -u webapp php php artisan tinker   # 対話実行
```

E2E は `e2e` プロファイルで立てた別インスタンスに対して走る。手順は
[tests/e2e/README.md](tests/e2e/README.md)。

# 作業の約束

## ブランチ

- 作業は話題ごとに切り、起点は必ず `develop`。
- 今いるブランチが今の話題と違うなら、そこへ積まずに `develop` から切り直す。
  名前の範囲を超えて伸びたブランチは、後から何を入れたのか読めなくなる。
- 名前は `fix/...`、`feature/...`、`docs/...`。
- `develop` と `master` へは直接 push しない。

## ブランチを切り替えるとき

`php` と `web` のコンテナはこのリポジトリを `./:/data` で直接マウントしている。
チェックアウトした内容がそのまま本番 (https://monelytics.me) の配信物になるため、
別のブランチへ切り替えると本番がそのブランチのコードで動く。

- 本番を止めずに別のブランチを触るなら `git worktree add` を使う。
  **スキーマを変える作業では必ずそうする。** ブランチを切って作業しただけで、
  新しいコードが旧スキーマの本番に配信され、ログイン後の画面が 500 になった
  ことがある。`/` と `/user/login` は対象の表に触れないので 200 を返し続け、
  気付きにくい。
- 切り替えたあとは Blade の再コンパイルが走る。`storage/framework/views` に
  php-fpm の実行ユーザ (webapp) 以外が作ったファイルが混ざっていると、
  `touch(): Utime failed` で 500 になる。`php artisan view:clear` で作り直す。
- 古いコミットへ切り替えると、後から追加されたファイルは git が消す。
  `tests/e2e` のスペックが丸ごと消えて E2E が走らなくなったことがある。
  戻すときは削除ではなくファストフォワードで戻す。

## コミット

- 1 つのコミットは 1 つの話題。ついでの修正は混ぜず、別のコミットに分ける。
- メッセージは英語。1 行目に何をしたか、本文に「なぜそうしたか」を書く。
  変更そのものは差分を読めば分かるため、残すのは差分から読めないこと。
- `git commit` は index にあるもの全てを載せる。`git rm` や `git mv` を先に
  実行していると、意図しないファイルが別の話題のコミットに紛れ込む。
  コミット前に `git diff --cached --stat` で中身を確かめる。

## 確認

- 画面の見え方を変えたときは、憶測で済ませず E2E で実際に測る
  (`tests/e2e/README.md`)。ずれの幅や余白は数字で確かめる。
- 挙動を変えたときは PHPUnit と E2E の両方を通してから push する。
- 本番データを持つ稼働環境で作業する。DB を変更する前にダンプを取る。
- 動作確認で作成したアカウントやレコードは必ず削除し、件数が作業前と
  一致することを確認する。

# コード規約

## 言語

- コード中のコメント、テストの docblock、`README.md` と本書は日本語。
- コミットメッセージと E2E のテスト名以外の識別子は英語。

## コメントは「なぜ」を書く

何をしているかは差分とコードが語る。コメントに残すのは、次に読む人が同じ
結論に至れない理由のほう。

- **検討して退けた案は、退けた理由ごと残す。** 例えば `Html::comparisonAmount` は
  増減を `▲▼` で表さない。日本の会計表記では `▲` が負の数を指すため、支出欄で
  「増えた」を `▲` で表すと意味が逆に読まれるから。理由が書いていないと、
  次の人が親切のつもりで戻してしまう。
- **そうした決定はテストでも固定する。** 上の例は `MacrosTest` が
  `+1,600` / `-900` / `±0` を直接検査している。コメントだけでは守られない。
- 仕様ではなく事故を防ぐための書き方（`?? null`、`whereNull`、上限）は、
  何を防いでいるのかを書く。書いていないと「冗長」に見えて消される。

## 層の分け方

- 業務ロジックは `App\Services`。コントローラは入力を受けて Service を呼び、
  View に渡すだけに保つ。
- 検索条件は `App\Libraries\Condition` のクラスにまとめ、配列を直接引き回さない。
- **同じ計算を層をまたいで書き直さない。** 月の区切りや前月の丸め方のような
  計算は、それを持っている側が結果を返す。`getMonthlyComparison` が比べた期間
  (`period`) を返すのは、View 側で `strtotime('-1 month')` を組み直すと
  3/31 に対する 2/28 の丸め方がずれるため。
- ルートには**実装のある action だけ**を登録する。`Route::resource` の既定は
  `index` や `show` まで載せてしまい、直接開くと `BadMethodCallException` で
  500 になる。`'only' => [...]` で絞る。
- 使われなくなったメソッドは消す。呼ばれない public メソッドが残っていると、
  次の人がそれを現役と読む。履歴には残る。

## 数と符号

- 支出は DB 上マイナス。画面に出すときにどちらの向きに揃えたのかを、
  返す側の docblock に書く。同じ画面で向きが混在していないか確認する。
- **前月との比較は率で出さない。** 元が小さい小項目は率が跳ね上がり
  (100 円から 300 円で +200%)、額の大きい小項目より目立ってしまう。
  逆に合計のように元の額が大きいところでは、1% 未満の増減が多く、率では
  いくら動いたのかが読めない。月別集計は前月の額そのものを並べる
  (支出が負の表で差額を出すと、差の符号をどちらへ向けても読み違える)。
  ダッシュボードは支出を正で出しているので差額を添えている。
- **金額には必ず「円」を付ける。** 例外はない。列見出しが「金額」と言って
  いても、表の外から数字だけを見る人には何の数か分からないため。表示は
  `Html::amount()` を通す。グラフの軸・吹き出し・ラベルも同じ。
- リンクになっている金額は `Html::amountLink()`。単位はリンクの外側に置く
  （単位はリンク先と関係がなく、含めると押せる範囲が数字より広がる）。
  間に空白を 1 つ挟む。リンクの下線は数字で止まるため、詰めると単位が下線に
  接して見える。
- **金額が 0 のときはリンクにしない。** 押しても絞り込みの結果は空で、開く
  意味がない。カレンダーのように 0 が大半を占める画面では、下線だらけになって
  実のある数字が埋もれる。
- 円でない単位（回、%）も書く。単位の有無で列の意味を見分けさせない。

## 一覧と表示

- 金額の入力欄には単位を接尾辞として置く（`input-group`）。そのとき
  **数値入力のスピナーは消す**。1 円ずつ動かす用途がないうえ、focus 中だけ
  現れて表示幅を奪い、桁の多い値の頭が隠れる。欄を広げて逃げると、常用する
  4、5 桁に対して空きすぎる。
- **利用者がいくつでも作れるもの（小項目など）を画面に並べるときは
  上限を設ける。** そして絞ったことを画面に書く。合計と並びが合わない理由が
  分からないと、数字が間違って見える。
- 並び順は同値のときも一意に決まるようにする。実行ごとに入れ替わると、
  読む人には理由のない変化に見える。
- 利用者が入力した文字列は `{{{ }}}` で出す。`{{ }}` も同じく逃がすが、
  三重は「これは利用者の入力」という印として使い分けている。
- 幅を固定した列に長い文字列が来る場合は、折り返させず省略して `title` に
  全文を持たせる。狭い画面で名前だけが何行にも伸びる。

## 安全側の既定

- 認証が要るものは `Route::group(['middleware' => 'auth'])` の中へ。外部へ
  取りに行く処理を持つエンドポイント（`/holidays` など）も同様に閉じる。
- 総当たりされうる POST には流量制限を付ける。値は `config('app.rate_limits.*')`
  に置き、環境変数で緩められるようにする（E2E は緩めた値で走る）。
- 削除は論理削除。JOIN した**全てのテーブル**に `whereNull('...delete_date')`
  を付ける。1 つ忘れると消したはずの行が集計に戻る。

# レビュー観点

過去に実際に不具合を出した箇所。コードレビュー時はここを必ず見ること。

## 同じ不具合が他にもないか

**最優先。** 不具合を 1 件直したら、同じパターンをリポジトリ全体で検索して
一緒に潰す。1 箇所だけ直して報告した結果、同種の不具合が別の画面に残っていた
ことが複数回ある。

- `&amp;` の二重エスケープ → `linkWithQueryString` を直したが
  `BaseCondition::buildQueryString` と `calendar.blade.php` の `link_to` に
  残っていた
- 表から列を削除 → 月別集計だけ数え直し、日別一覧と変動費登録の colgroup が
  壊れたまま
- 所有者スコープ → `edit` と `destroy` は絞ってあったが `update` だけ
  素通しだった

## 認可とスコープ

- レコードを取り出す経路は**全て** `user_id` で絞る。`findOrFail($id)` だけで
  取ると、`PUT /cost/variable/{id}` が他人の収支を書き換えられた。
- 付け替え先（小項目など）も同じように絞る。本体を絞っても、
  他人のカテゴリへ移せてしまう。
- 1 つの action を直したら、同じリソースの残りの action も並べて見る。

## 入力の検証

- **検証ルールを列の型と突き合わせる。** int 列に `numeric` を当てていたため、
  `'1.5'` が 2 に、`'1e5'` が 100000 になり、桁あふれは未処理の DB エラーに
  なっていた。`integer` と列の範囲を指定する。
- 同じ項目のルールを画面ごとに書き直さない。片方だけ直って食い違う。
- 並び替えの列名やソート方向のような、そのままクエリに入る値は
  受け付ける値を定数で閉じる（`DailyPaginateCondition::SORT_FIELDS`）。
- LIKE 検索は `%` と `_` を逃がす。`addslashes` は別物で、`'%'` が全行に
  当たっていた。

## リンク・URL

- `http_build_query` の区切りは `&`。HTML への逃がしは `Html::link` / `link_to`
  が行うため、`&amp;` を渡すと二重になり、2 つ目以降のパラメータ名が
  `amp;xxx` になって黙って無視される。絞り込み条件が落ちても画面はエラーに
  ならないので気付きにくい。
- 自前で `<a href="...">` を組み立てる場合（`Html::sortLabel` など）は逆に
  `&amp;` が正しい。**どちらの層でエスケープするのか**を必ず確認する。
- クエリ文字列の値は HTML エスケープ（`e()`）ではなく URL エンコードを使う。

## 表

- 列を増減したら **colgroup / thead / tbody / データ無しの行 / tfoot の
  `colspan`** を全て数え直す。`<colgroup span="N">` と `<col>` の数え方が
  混在しているので、実際に描画して数えること。
- 列を消したら、対応するセル（空の `<td>` が残りがち）と幅指定も消す。
- 単位や記号を足したら列幅を見直す。5% の列に「円」を足して、単位だけが
  次の行に落ちていたことがある。
- 幅が足りないように見えたら、まず何が幅を食っているのかを測る。数値入力の
  スピナーが原因なのに列を広げて、空きすぎにしたことがある。

## アセット

- `public/assets/js` `public/assets/css` を変更したら `Html::versionedScript` /
  `Html::versionedStyle` を使う。nginx は `/assets` に `Cache-Control` を
  返さないため、素の `Html::script` ではブラウザが古いファイルを使い続ける。
- **JSON の応答形式を変えたら、それを読む JS と必ず同時にレビューする。**
  片方だけ変わるとキャッシュされた旧 JS が例外を投げ、画面が無言で壊れる。
- そのプラグインを使っている画面が無くなったら、`head_tags.blade.php` からも
  外す。誰も使わないものを全画面で読み込み続けることになる。

## 画面

- 桁区切りは明示する（`Highcharts.numberFormat(v, 0, '.', ',')`）。
  既定に任せて空白区切りになっていたことがある。
- 値が片側にしか出ない軸は、空いている側を描かない（`min: 0` など）。
- UI の状態保持は揃える。どちらも保持しないと壊れているように見える。
  タブは `startTabs` が URL のクエリ (`tab=`) に持つ。画面そのものを指す
  状態なので、URL を渡した相手にも同じタブが出る。同じ画面を 2 つ開いても
  互いを引きずらない。グラフの中の絞り込みのように画面を指さないものは
  `rememberSelect` でクッキーに持つ。
- `col-*` は `.row` の中に置く。Bootstrap 5 では横並びのラベルは
  `.row.mb-3` + `.col-form-label`。
- jquery-ui の base テーマを打ち消すときは**詳細度を揃える**。
  `.ui-tabs .ui-tabs-nav .ui-state-active` (0,3,0) では
  `li.ui-tabs-active` (0,3,1) に負け、指定が黙って無視される。
  効かない CSS を見たら、まず計算後の値を測って当たっているか確かめる。
- 送信先が受け付けないものを選択肢に出さない。ダッシュボードのかんたん入力は
  `cost/variable` に送るのに固定収支の小項目まで並べていた。

## PHP 8

- 任意項目は `?? null` で守る。未送信の配列キーへのアクセスは警告が例外化し、
  処理ごと落ちる。
- `count()` / `strlen()` に null を渡さない。null を返しうるメソッドは
  空配列を返すようにする。

## SQL

- `ONLY_FULL_GROUP_BY` が有効。SELECT する非集約列は全て GROUP BY に入れる。
- GROUP BY には**列の別名ではなく実際の列名**を書く（別名は受け付けられない）。
- MariaDB に `ANY_VALUE` は無い。1 件に畳むときは `MIN()` などを使う。

## スキーマ

- **本番のスキーマは migration から作った DB と同じとは限らない。** 2015 年に
  作られた本番の timestamp 列は既定値が `'0000-00-00 00:00:00'` で、
  migration が書いた `CURRENT_TIMESTAMP` は入っていない。Laravel は
  `'strict' => true` で `NO_ZERO_DATE` を立てて接続するため、その表へ
  `ALTER TABLE` を投げると「Invalid default value」で落ちる。新しく作った DB
  には正しい既定値が入るので、**PHPUnit も E2E も通って本番だけ落ちる。**
- 列や表を改名する migration を書いたら、本番の構造を写した DB で先に試す。
  `mariadb-dump --no-data` と `migrations` 表のデータだけを別 DB へ流し込めば、
  本番と同じ状態を作れる。
- migration は途中まで通った状態から再実行できるようにする
  (`Schema::hasTable` / `hasColumn` で守る)。DDL は巻き戻らないため、
  複数の文を持つ migration が落ちると中途半端な schema が残る。

## 設定

- フレームワークの既定値をそのまま受け取らない。Laravel 13 の既定は
  `DB_CONNECTION=sqlite`、`REDIS_CLIENT=phpredis`（イメージに無い）、
  `CACHE_STORE`/`SESSION_DRIVER=database`（対応する migration が無い）で、
  古い `.env` はそこへ素通しで落ちる。
- 版を上げたら、リネームされた設定キーを README に書く。旧名は読まれない。

## 状態を持つオブジェクト

- モデルのプロパティ（`$rules` など）を検証のたびに書き換えない。
  同一インスタンスが使い回されると、後続の検証が前の設定で走る。
  用途ごとに異なるルールは引数で渡す。

## テストそのもの

- **テストが何も確かめていないことがある。** `assertValidAjaxResponse()` は
  bool を返すだけで、5 箇所の呼び出しが全て戻り値を捨てていた。
  ヘルパは値を返さず、中で assert する。
- **fixture を疑う。** `ActivityTableSeeder` は変数名の打ち間違いで 8 件中
  4 件しか作っておらず、それに乗ったテストは説明の半分のデータで通っていた。
  期待値が合わないときは、まず種データが説明どおりかを見る。
- PHPUnit は PHP 側のロジック、E2E はサーバが返す HTML と JS の継ぎ目。
  同じことを二重に書かない。率や金額の正しさは PHPUnit、ajax で差し込まれた
  断片が画面に届いているかは E2E。
- E2E のセレクタに Bootstrap のクラス名（`.btn`、`.col-md-4`）を使わない。
  版を上げたときに一斉に落ちて、何が壊れたのか分からなくなる。
- テストの docblock には、何を確かめているかではなく**なぜそれを確かめるのか**
  （過去に何が壊れたのか）を書く。
