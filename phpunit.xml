<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Application Test Suite">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- リダイレクト先の比較で app.url と生成 URL を一致させる
             (旧 TestCase の $baseUrl = 'http://localhost' に相当)。 -->
        <env name="APP_URL" value="http://localhost"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <!-- マイグレーションと初期データ投入を伴うため、SQLite ではなく
             本番と同じ MariaDB の検証用スキーマを使う。 -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="monelytics_testing"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <!-- 問い合わせ送信テストは宛先が空だと RfcComplianceException になる。 -->
        <env name="NOTICE_CONTACT" value="notice@example.com"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
    </php>
</phpunit>
